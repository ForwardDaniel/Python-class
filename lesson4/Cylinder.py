import math

def area (radius,height):
    print ("The Area Of The Cylinder Is",2 * math.pi * r * h + 2 * math.pi * math.pow(r, 2))
    return (area)


print ("Enter The Radius")
radius= float(input())
print ("Enter The Height")
height= float(input())
r=radius
h=height
area(radius, height)
